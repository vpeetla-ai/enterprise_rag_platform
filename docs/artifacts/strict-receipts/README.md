# docs/artifacts/strict-receipts/

Dated Strict ERAG panel evidence from `scripts/capture_strict_panel_receipt.sh`.

Do **not** commit `RAG_JWT_SECRET` or raw JWTs. Health snippets with `review_mode=strict` are OK.
